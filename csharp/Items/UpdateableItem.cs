using System;

namespace csharp.Items
{
    /*
    public class UpdateableItem : IItem
    {
        private readonly Item _item;
        private readonly ChainableUpdateRule _rule;

        public UpdateableItem(Item item, ChainableUpdateRule rule)
        {
            _item = item ?? throw new ArgumentNullException(nameof(item));
            _rule = rule;
        }

        public void Update() => _rule.GetRule();
    }
    */
}